<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <p><input type="checkbox" value="<% task.completed %>"><h5><%= link_to task.title, task_path(task.id) %></h5></p>
<% end %>
<p><%= link_to 'Create a new task', new_task_path %></p>
